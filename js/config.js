/**
 * Application Configuration & Supabase Credentials
 * You can put your default Supabase settings directly in this file,
 * OR configure them dynamically through the Settings screen in the web app.
 */

export const CONFIG = {
  // Supabase Configuration
  SUPABASE_URL: '',      // e.g. 'https://yourprojectid.supabase.co'
  SUPABASE_ANON_KEY: '',  // e.g. 'eyJhbGciOiJIUzI1NiIsInR5cCI6...'
  SUPABASE_ENABLED: true  // Set to true to automatically enable sync when URL & key are provided
};
